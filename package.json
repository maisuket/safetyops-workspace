{
  "name": "safetydock-workspace",
  "version": "1.0.0",
  "description": "Workspace raiz para o sistema ITAM HUB / SafetyDock",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run start:dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently -c \"cyan.bold,green.bold\" -n \"NESTJS,REACT\" \"npm run dev:backend\" \"npm run dev:frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
